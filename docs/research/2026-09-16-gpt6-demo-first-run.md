# GPT-6 首轮仿真抓放：实际结果与下一步

2026-09-16，工程闭环已跑通；**第一次完整任务尝试未通过 LIBERO 官方评分**。这份记录只描述一次 demo 调试，不声称成功率或研究贡献。

## 固定条件与结果

- LIBERO spatial task 0，init 0，seed 0；任务为将盘子与小碗之间的黑碗放到盘子上。
- GPT-6 Astra，官方 Codex CLI 0.154.0-alpha.6.2、当前 ChatGPT 登录、low 推理；双相机 RGB512、`current` 上下文、`tcp_target_servo_v2`、controlled 时序。
- 运行编号 `20260916-163124-59f9b159`，comparison group `e8b39c3a6d76bebf`。本轮运行中没有人工改变任务或动作。
- 24 次请求均产生可解析动作：19 次 move、5 次 gripper；执行终态为 reached 10 次、timed_out 9 次、completed 5 次。
- 389 个官方控制步，19.45 秒仿真时间，墙钟约 5 分 42 秒；请求耗时合计 314.426 秒，平均 13.101 秒。
- 官方 `libero_check_success=false`；终止原因 `decision_budget`，没有协议错误，也没有模型 done 声明。

此前文字、单图和双图 wait 格式诊断通过。单动作验收请求上移 5 毫米，实际 Z 位移 4.398 毫米，位置误差 0.638 毫米，7 个控制步后 reached 并暂停。这些检查分别验证通信、格式和执行，不代替任务评分。

日志中当前任务报告的用量为 input_tokens 308638、cached_input_tokens 21504、output_tokens 4289；这是该任务的 CLI 用量字段，不是整次开发的账户消耗，也没有换算成价格。整个首轮按 34 次策略提交上限收口：包含联调失败的 7 次文字请求、2 次后续诊断、1 次小位移、24 次任务决策。CLI 内部网络重试不等于策略提交数。

## 从轨迹能看到什么

1. 第 1–9 次决策接近目标、张开夹爪并下降。有些 timeout 的残差很小，另一次下降残差约 12 毫米；不能将它们都解释成同一种失败。
2. 第 10 次闭合，第 11 次抬升。第 12 次模型根据图像与近乎完全闭合的夹爪判断抓取不可靠，选择张开重试。
3. 第 13–17 次向碗沿重新对齐、下降、闭合和抬升。后续图像有物体靠近夹爪并随位置变化的迹象，但这里没有单独标注“稳定抓持”真值，不能仅靠最终失败否定整个抓取阶段。
4. 第 18–23 次模型沿盘子方向移动并下降，第 24 次张开夹爪。末段理由主要使用“贴近夹爪”“碗与盘子重叠”等证据；官方任务最终未成功。

目前只能确定“闭环执行和重试发生了，任务未完成”。是否最终放置偏心、物体姿态不合适、接触关系未满足，或者需要进一步调整，尚未通过独立逐帧标注区分。不能用一张遮挡严重的投影图或一个布尔评分给整个失败链定性。

## 建议继续的第一步

先离线回看第 10–12、16–18、22–24 次决策，对每一步记录三个问题：模型声称看到了什么、动作打算验证或改变什么、后续图像支持该判断吗。评分标签只放入独立分析表，不回传策略。

一个可证伪的候选问题是：**在相同动作接口与请求次数预算下，加入最近一次动作前的双相机证据，是否减少“仅有图像重叠／邻近，就继续执行下一子步骤”的错误判断？**

下一轮可固定任务、初始化与模型，对比 `current` 和已实现的 `paired`。应同时报告任务结果、判断证据一致性、失败恢复行为、控制残差及额外图像带来的用量；请求次数相同不等于 token 预算相同。当前只有一次基线记录，没有执行这组对照，不能归因“paired 更好”。文献新颖性、正式实验预算与论文方案仍需另行收敛。

本机完整帧回放、传感输入、上下文、动作/反馈、独立摘要和与 manifest 匹配的公开源码快照保存在忽略的 `runs/gpt6-demo/20260916-163124-59f9b159/`。后续修复的是暂停、深度快照和旧回放保护，不覆盖这次运行的结果。
