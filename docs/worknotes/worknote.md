# ManiLoop 工作记录

## 当前状态

- M15：GPT-6 完整新 episode 已通过 LIBERO 官方成功判定：46 次决策、706 控制步、35.30 秒仿真；707 组双相机完整录像已成为[官网主演示](https://daniel-rmc.github.io/ManiLoop/#demo)。源码同步 GitHub，三平台测试与 Linux 双机器人渲染全部通过；公网首页和 39 个文件一致性核对通过。详情见 [成功记录](../research/2026-09-16-gpt6-success-episode.md)。

- M14：v0.2.0 开源快照与 [GitHub Pages 展示页](https://daniel-rmc.github.io/ManiLoop/) 已发布。框架提交 35713b8、站点提交 ff21875 均通过三平台测试与 Linux 双机器人渲染；公网首页和 22 个文件 HTTP 200 且与本地逐字节一致。展示真实首轮失败记录，不发布私人配置、权重、环境和原始实验目录。

- M13：GPT-6 demo 已实现并实测：Codex 登录、真实文字／单图／双图格式、5 毫米动作均通过；单步／暂停／配对视觉／离线回看可用。首轮官方 task 0 / init 0 / seed 0 抓放在 24 次决策后未成功。完整离线 180 项及 136 项子测试通过，真实 LIBERO 14 项通过；首轮含联调失败共 34 次策略提交，已停止继续请求。详见 [使用说明](../GPT6_DEMO.md) 与 [首轮结果](../research/2026-09-16-gpt6-demo-first-run.md)。
- M12：GPT-6 仿真操作 demo [改造计划](../design/GPT6_SIM_DEMO_PLAN.md)已完成；该条记录方案编写时状态，后续实施和真实验证见 M13。
- M11：本地 LeRobot 恢复完成；独立 VLA 环境与三个固定检查点已安装、38 文件完整性校验通过。新电脑 MPS 单任务验收：SmolVLA 78 步成功、DP 79 步成功、ACT 500 步预算内未成功；网页本地启动接口已实际通过。
- M10：新电脑恢复完成且用户已人工验收；macOS 15.7.9 / Apple M4 / 24 GiB。主环境 144 项测试及 58 项子测试通过，13 项真实 LIBERO 集成检查另行通过；聊天 8769、LIBERO 8767 已启动。旧原始轨迹和私人配置未恢复；云端图像／动作仍待验证，VLA 恢复见 M11。
- M10c：执行反馈候选已做五篇近邻预检，形成[选题讨论材料](../research/2026-09-16-question-scoping.md)；问题、预算与方案尚未确定，未启动论文实验或训练。
- v0.1：自建 MuJoCo 双机器人桌面框架已完成。
- LIBERO 接入：首轮交付完成。复用官方任务、初始化、Panda 控制器与成功判定，通过独立环境适配接入 CLI 和网页。
- M1：确认边界与建立工作记录（完成）。
- M2：官方运行环境与任务验证（完成）。
- M3：共用环境接口、LIBERO 适配器与用户入口（完成）。
- M4：集成测试、安装说明和交付核对（完成）。
- M5：本地真实策略验证（完成）：官方 SmolVLA、社区 ACT / Diffusion 均已真实运行；同一官方任务上 SmolVLA / Diffusion 成功，ACT 预算内未成功。
- M6：Git 管理与公开仓库同步（完成）：[Daniel-rmc/ManiLoop](https://github.com/Daniel-rmc/ManiLoop)，主分支 `main`。
- M6a：三平台离线测试与 Linux OSMesa 渲染均已在 GitHub Actions 实际通过。
- M9a：提交身份与远端账号关联已修正；用户选择保留历史，在 GitHub 账号添加旧邮箱以关联此前 10 条提交。
- M9：跨电脑迁移交接已整理，包含安装入口、非 Git 数据清单、研究待决事项与接手说明。
- M8：Show-Harness 源码与论文工程分析已完成；语义动作策略仍为设计建议，尚未接入或实测。
- M8a：补充模型、作者自建 harness 与厂商浏览器 agent 的调用边界；核实 GUMI 模拟完成信号不可直接进入传感器赛道。
- M7b：独立 API 聊天页已完成，用户提供配置的 FC / Sol 文字通信真实通过；SDK 空错误对象误判已复现并修复。
- M7：云端诊断完成；M7a 的分层请求诊断、目标控制、LLM 图像配置和网页交付已完成。用户随后的两次云端复测均在响应验证阶段停止；尚未得到可执行动作或任务成功。

### 2026-09-16 · M15 · 成功 episode 与官网演示（完成）

- 授权：用户要求完成一次成功的 GPT 仿真操作，保存从初始化到独立成功终止的同一条完整 episode，并将官网主演示换成成功运行。既有 Codex 登录通道授权继续有效，不读取或复制认证内容。
- 基线：v0.2.0／8e728a2，main 与远端一致，工作区干净。首轮在第 24 次松爪后耗尽决策预算，没有释放后的再观察余量；这不是成功结论，也不证明只需多走一步。
- 计划：先保持官方 spatial task 0／init 0／seed 0、RGB512、TCP servo 与独立评分，采用 paired 传感上下文、medium 推理。用户随后明确要求取消人为决策上限，因此本次使用 max_calls=0；由独立成功或官方环境终止结束，保留 3600 秒墙钟保护。此为 demo 开发选择，不作为严格消融或成功率评测。按实际失败证据决定后续，暂不批量扫任务或训练。
- 提示词：补通用的抓取后随动核对、释放后退离再观察，以及单视角投影重叠不足以确认成功的说明；不提供物体坐标、具体轨迹或评分。不据这次开发同时改变的多项条件作因果归因。
- 记录：增加显式可选的完整 LIBERO episode 录制，保存初帧及每个真实官方控制步的双相机和时间信息，与最终独立结果绑定；原始记录仍置于忽略目录。官网只发布经复核的演示产物。
- 验证：录制开／关的相同 8 个官方控制步，本体轨迹逐位一致；保存初帧加 8 帧，与独立评分计数匹配。录制离线完整性检查、CLI 短 episode 与 MP4 解码已通过。不限决策的调度仍在环境终止时停止且不向策略传入评分，墙钟边界仍有效。
- 实测成功：运行 `20260916-172331-d3bc5ef2`，46 次有效 GPT 决策、706 官方步、35.30 秒仿真、667.33 秒墙钟；initial false → success/current_success/terminated 全 true，自动终止后没有额外请求、人工暂停或在线配置修改。
- 完整性：707 组双相机、1414 图片及索引全部 SHA 校验通过，三处独立评分一致，92 份策略观测／上下文通过边界检查；运行源码与冻结摘要一致。MP4 独立解码为 707 帧、20 fps、35.35 秒（包含初帧）。本次没有批量筛选或训练。
- 判定范围：成功在第 46 次下降中触发，环境随即停止；未额外执行松爪／退离，不把官方成功扩张为完全释放后稳定放置的额外验证。原始首轮失败及本轮重试均保留。
- 工程验证：完整离线 194 项及 136 项子测试通过，15 项可选集成跳过；录制开／关相同动作的真实集成另行通过。发布前只清理录制描述中的默认适配器文案，未改原始运行或实际控制。
- 发布前复核：元数据清理后的录制与 LIBERO 定向检查 26 项通过；16 张精选图、8 段动作说明和完整视频与原运行逐项一致，文件摘要与索引通过。变更文本未发现凭据，原始 runs 保持忽略。真实 Chrome 已播放完整 MP4，成功标记、707 帧／20 fps 和未额外松爪退离说明可见；公网部署待完成。
- 已发布：源码与站点提交 `9878d5c` 正常推送 main；[CI](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35081452540) 的 Ubuntu／macOS／Windows Python 3.12 测试与 Linux OSMesa 双机器人渲染均成功。站点通过 subtree 正常更新 gh-pages 到 `c2df569`，[Pages 部署](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35081463372) 成功，原 v0.2.0 标签及历史未重写。
- 最终验收：公网首页和全部 39 个公开文件 HTTP 200、逐字节匹配已审阅 site；MP4 为 video/mp4，6,750,773 字节，摘要一致。本地真实 Chrome 显示视频从 17.166 秒推进至 35.35 秒末尾、播放按钮恢复，精选记录可到 8/8、STEP 46。用户开始操作浏览器后停止交互；本轮未补做新视频的手机布局、控制台及公网交互验收，未将这些写为通过。
- 后续：本轮 demo 目标完成；正式研究前先冻结条件、另行确定少量初始化复验与对照预算。一次官方成功不能证明稳定成功率，也不能单独归因于 paired 上下文。

### 2026-09-16 · M14 · v0.2 开源快照与 GitHub Pages（完成）

- 授权：用户要求将当前初步框架和 demo Git 保存并同步 GitHub，建立 github.io 项目展示页；此授权涵盖提交、推送与公开站点发布。
- 基线：当前功能分支与 origin/main 同为 1d5ea30，获取远端后无分叉，保留全部既有工作；提交身份 Daniel-rmc 已正确配置，不重写历史、不强制推送。
- 整理：版本设为 0.2.0；补最新交接状态，修复新测试的 Windows 绝对路径与 symlink 权限假设。展示页使用独立 site 目录，只选择 8 个真实运行画面节点、16 张相机图和最小动作摘要，明确官方任务未成功。原始 runs 与模型文件保持忽略。
- 发布：源码在 main，静态展示源文件为 site/；使用 git subtree 正常推送 gh-pages，GitHub Pages 从该分支根目录构建，HTTPS 已启用。项目地址 https://daniel-rmc.github.io/ManiLoop/，同步写入仓库主页入口。
- 已同步：框架提交 `35713b8` 正常推送 main；本地离线 183 项及 136 项子测试通过（14 项可选 LIBERO 检查跳过）。该提交的 [GitHub Actions](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35076094827) 已完成：Ubuntu／macOS／Windows Python 3.12 测试与 Linux OSMesa 双机器人渲染全部通过。
- 站点验收：16 张公开 JPEG 与真实回放逐一哈希一致；8 组动作解释和反馈与原记录相符，未含私人路径或凭据。真实浏览器验证桌面布局、双相机翻帧、滑杆首末边界、播放推进、命令复制；390px 手机宽度无横向溢出，浏览器无错误。JS 语法、静态相对资源和锚点检查通过。
- 部署证据：站点源码提交 `ff21875` 的 [CI](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35077155415) 全部通过；发布树 `6ed73af` 的 [Pages 构建与部署](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35077194641) 成功。公网首页和全部 22 个公开文件（HTML／CSS／JS／JSON／SVG／JPEG／说明）HTTP 200，内容与本地逐字节一致。
- 验证范围：浏览器交互验收在本地完成；随后内置浏览器会话超时，公网以 HTTPS 响应、部署状态及全部文件一致性验收，未声称完成额外的公网浏览器交互测试。
- 维护：site/README.md 记录预览与后续发布命令。使用 v0.2.0 标签保存本轮版本；原历史保留，未强推、未发起新模型请求。下一步仍是核对失败阶段与确定小规模研究对照，不将网站发布当作稳定抓放或完整基准验证。

### 2026-09-16 · M13c · GPT-6 闭环演示交付与首轮抓放（工程完成，任务未成功）

- 实测：5 毫米上移动作返回 reached，实际 Z 增量 4.398 毫米、位置误差 0.638 毫米，7 个控制步后按预期暂停。随后官方 spatial task 0 / init 0 / seed 0 从初态运行，current 双图、low、TCP servo、controlled 时序，24 次有效决策（19 move、5 gripper）、389 控制步、19.45 秒仿真时间；官方评分 false，终止原因为 decision_budget。
- 过程：出现首次闭合／抬升后主动张开重试、第二次对齐碗沿，以及向盘子方向移动并释放；这些动作和模型解释不等于稳定抓持或任务成功。10 次 reached、9 次 timed_out、5 次 gripper completed；详见首轮结果文档，不将不同 timeout 当作同一失败。
- 证据：`runs/gpt6-demo/20260916-163124-59f9b159/` 保存 49 帧回放、观测与上下文、完整事件、独立摘要、用量统计，以及和当时 manifest 源码摘要一致的公开代码快照。比较组 e8b39c3a6d76bebf。后续工程修复保留该次完整结果，不抹除失败记录。
- 修复：保护已结束实验，reset／configure／重复 stop 不再覆盖旧评分和回放；配置切换丢弃响应仍计入单步并暂停；done 清理暂停标记；回看采集不替换策略快照，暂停恢复不丢失深度查询。新增定向回归均通过。服务重启可恢复相同输出目录中最近的回放。
- 最终验证：`.venv/bin/python -m pytest -q` 为 180 passed、14 skipped、136 subtests passed；`MANILOOP_TEST_LIBERO=1 .venv/bin/python -m pytest tests/test_libero_integration.py -q` 为 14 passed。真实浏览器验证新控件、512 双图、49 帧回放及上一帧导航；三个网页地址 HTTP 200，重启后的回放字节与保存文件一致。源文件空白与文档链接检查通过。
- 交付：LIBERO demo `http://127.0.0.1:8767/`，最近回放 `/replay/current`，独立 API 聊天 `http://127.0.0.1:8769/chat`。当前服务从新初始化待机，Codex 来源已预选，不自动请求模型。聊天页仍使用用户自行配置的 API 通道。
- 预算与限制：首轮按 34 次策略提交收口，包含早期认证／版本／解析联调；CLI 内部网络重试另由客户端管理。没有追加 paired 真实模型对照或 init 1/2 复验，没有训练、下载新权重、修改官方任务与评分、复制 token、提交或推送。
- 下一步：先离线核对两次抓取和最终释放阶段的判断与视觉证据，再固定预算比较 current 与 paired。首轮未成功，不能承诺稳定自主抓放，研究问题与正式预算仍待确定。

### 2026-09-16 · M13b · GPT-6 真实图像与结构化动作（通过）

- 通道：GPT-6 Astra，经官方 Codex CLI 0.154.0-alpha.6.2、ChatGPT 登录、禁用工具、low 推理；模型真实返回 READY。修复已知启动告警后保留其他错误和工具事件的严格拒绝。
- 实测：单图诊断 7.344 秒返回对机械臂、夹爪、桌面、炉盘、碗、盘子与抽屉柜的描述；双图动作诊断 9.497 秒返回严格 wait JSON。两轮 api_calls=1、执行动作=0、仿真时间=0，均明确 executed=false。
- 独立集成：新增无网络 fixture 策略检查，在真实 LIBERO RGB512 / TCP servo 上完成两次单步、四图配对、最终本体反馈和暂停物理验证，1 项通过；与此前 13 项合计 14 项真实 LIBERO 检查通过。
- 证据：新记录保存在忽略的 runs/gpt6-demo 及 .runtime 诊断摘要中；不会上传账户认证或旧机器私人记录。通信及动作格式通过不代表任务完成，下一步测试 5 毫米上移和完整抓放。

### 2026-09-16 · M13a · Demo 控制、回看与离线验证（完成；真实推理联调中）

- 变化：新增独立 Codex 登录通道；增加当前／前后双相机上下文、最终反馈去重、单步／动作后暂停／继续、独立回放 HTML、CLI 上下文参数与网页抓放预设。停止会取消 Codex 子进程，旧请求不能执行。
- 隔离：整个观测／历史／几何请求检查嵌套特权字段；最终反馈只允许本体控制字段；评分只进入人类摘要与回放。Codex 在临时传感目录中运行，禁用工具和本机 skill，不提取认证内容。
- 验证：完整离线回归 165 项通过、121 项子测试通过、13 项可选检查跳过；随后 `MANILOOP_TEST_LIBERO=1 .venv/bin/python -m pytest tests/test_libero_integration.py -q` 的 13 项真实 LIBERO 检查通过。网页 JS、单步按钮状态、来源检查与回放路由通过；真实浏览器已显示 RGB512 双相机与新控件。
- 联调发现：先遇到已撤销的 CLI 刷新令牌；用户重新登录后服务器转为明确拒绝旧 CLI 0.144.1。选择本机应用自带官方 0.154.0-alpha.6.2 后，GPT-6 已真实返回 READY，但精确的“code mode host disabled”启动告警导致初版解析器拒收；正补只允许该已知启动告警的回归，不启用工具、不放宽真实错误校验。
- 边界：以上是工程与通信证据，尚无 GPT 图像／动作或任务成功结论。未克隆第三方框架、训练、改写历史或推送。后续按剩余有界预算继续图像诊断、单动作与单任务尝试。

### 2026-09-16 · M13 · GPT-6 Demo 实施与 Codex 登录通道（进行中）

- 授权：用户要求直接完成计划，并明确允许使用当前 Codex 登录调用 GPT。先执行有界诊断和闭环；不训练、不扩大为论文实验。
- 起点：保留工作区既有修改。`codex --version` 为 0.144.1，`codex login status` 返回 Logged in using ChatGPT；CLI 提供 `--ignore-user-config`、图像、输出 schema 和 ephemeral 模式。
- 设计：增加独立 Codex provider，官方 CLI 管理登录，不将登录 token 当作普通 API Key；隔离工作目录，关闭 agent 外部工具，只显式传感输入。现有 API Key 和本地 LeRobot 通道保留。
- 后续：离线验证通道与请求边界，真实分层诊断和单任务；补前后证据、单步／连续执行与回看。所有模型与任务通过状态以实际结果补录。

### 2026-09-16 · M12 · GPT-6 仿真操作 Demo 计划（方案完成，实施未开始）

- 目标：根据用户要求，将七个项目的研读结论落到 ManiLoop 的可审阅改造计划，遵循先完成再完善。
- 变化：新增 [计划](../design/GPT6_SIM_DEMO_PLAN.md)及研究索引；核对当前工作区的 Responses 严格 JSON、RGB512、本体固定目标执行器、历史、网页与记录入口。保留既有未提交修改。
- 发现：控制链主体已存在，真实云端图像／动作和任务能力仍待验证；模型当前收到本轮图像，近期历史主要为动作／文字反馈。新增前后帧与嵌套反馈应有独立版本和请求边界验证，不假定既有顶层允许列表涵盖新增内容。
- 方案：D0 固定条件，D1 原链路分层验证与单任务，D2 按失败证据补上下文，D3 单步／反馈／回看，D4 冻结配置后少量初始化复验。以官方 spatial task 0 为第一项建议，推物／抽屉／VLA 协作放在后续。调用数量、工期与验收目标均为建议。
- 验证：本轮仅只读核对代码、任务文件名称及文档，并在线记录七个参考仓库的固定提交。三个文档的本地链接、代码围栏与空白检查通过，`git diff --check` 通过；未运行仿真、功能测试、模型或付费 API。
- 边界：不修改官方任务、初始化、控制器、缩放或评分；策略不能读取调试 `/api/state`、评分或审计文件。没有使用本机 skill、克隆第三方仓库、安装依赖、读取私人配置或提交／推送。
- 后续：用户审阅后再进入实施，配置与预算确定后才进行真实云端验证；本文不代表已实现新 demo 或选定论文课题。

### 2026-09-16 · M11b · 三模型 MPS 实测与交付（完成）

- 环境：Apple M4／24 GiB；独立 `.venv-vla` 的 Python 3.12.14、LeRobot 0.4.4、PyTorch 2.10.0、torchvision 0.25.0、transformers 4.57.6。100 包一致性检查与 MPS 实际运算通过；主环境／LIBERO 的安装前后 freeze 完全一致。三个固定 checkpoint 的运行时 weight SHA256 与下载校验记录相符，均 `device=mps`、`strict_weights=true`。
- 协议：固定 `libero_spatial` task 0／init 0／seed 0、controlled、双 RGB256、8 维本体、官方 OSC／初始化／评分；每模型只跑一次完整单任务验收，不训练、不调参，不传入物体／接触／任务成功真值。ACT／DP 是非语言条件的视觉模仿模型，SmolVLA 支持语言。
- 实测结果：

  | 模型 | 入口 | 控制步 | 真正模型推理次数 | 仿真秒 | 官方成功 | 终止原因 |
  |---|---|---:|---:|---:|---|---|
  | SmolVLA | 独立网页 HTTP 服务 | 78 | 2 | 3.90 | 是 | 官方环境终止 |
  | ACT | CLI benchmark | 500 | 5 | 25.00 | 否 | 仿真预算 |
  | Diffusion Policy | CLI benchmark | 79 | 10 | 3.95 | 是 | 官方环境终止 |

- 预算与时间：三者调用上限 500。CLI 明确设置 25 秒仿真／1800 秒墙钟；网页沿用服务默认 120 秒仿真／600 秒墙钟，受 500 个原生控制步限制。ACT／DP 的加载后墙钟分别为 66.13／57.35 秒；网页结果不伪造同口径墙钟。不同入口／后台负载与旧硬件不可直接作性能比较。
- 运行边界：三者无运行错误；裁剪标量数依次为 53／251／0。成功后 phase 同样为 stopped，结论以独立 `evaluation.success` 和终止原因判断。新机器 SmolVLA 78 步／DP 79 步与旧记录 79／108 步分别保留，不要求跨机器轨迹逐位一致；不能估计整个 LIBERO 成功率。
- 证据：ACT `runs/vla-recovery-20260916/20260916-151350-b66f34b1`，DP `runs/vla-recovery-20260916/20260916-152012-fa4e22cf`；SmolVLA 的网页 manifest／events／末帧和摘要见 M11a。下载、安装、环境、完整校验、定向 19 项离线回归与验收摘要均保存于 `.runtime/recovery/`；原始数据被 Git 忽略，本次未恢复旧电脑 runs。
- 交付：原 LIBERO 页面 `http://127.0.0.1:8767/` 继续运行，选择「本地 LeRobot · 真实模型」再选 SmolVLA／ACT／Diffusion，保留官方任务，点击开始即可，不需要 API Key。临时 8768 验收服务已关闭；本次没有更改生产控制代码、官方基准或模型内容，没有云端模型调用或训练，没有提交／推送。
- 后续：用户可按模型切换继续本地调试。后续多任务／多初始化评测、研究假设和预算另行确定；本里程碑不改变云端图像／动作未验证和九动作仍为设计的状态。

### 2026-09-16 · M11a · 权重完整性与 SmolVLA 网页入口（完成）

- 下载结果：SmolVLA／ACT／Diffusion 各 9 个文件，加上 SmolVLM tokenizer 的 11 个 JSON／TXT，共 38 个固定版本文件，合计约 2.19 GB，全部通过大小及 Git blob／LFS SHA256 校验；没有残留 `.partial`，没有重复基础 VLM 权重。三个来源 sidecar 与完整校验记录已保存。
- 安装复验：原 `setup_vla.py --models smolvla-libero act-libero diffusion-libero` 重跑退出 0；HEAD 元数据失败时复用已独立校验的本地文件。主环境／LIBERO 环境的安装前后 freeze 完全相同，推理保持独立。DP `pretrained_backbone_weights=null`，保留两帧历史、8 步队列、原始 100 步采样。
- 网页验证：浏览器标签在检查过程中失效，改用临时 8768 本机服务验证同一个 `/api/start` HTTP 入口；未改用户 8767 页面状态。本次发送 `agent=lerobot`、SmolVLA、显式 MPS、500 步上限，不含云端配置；实际采用 RGB256 与原生 OSC。临时服务验收后已关闭。
- 实测：SmolVLA 严格完整加载，78 个控制步／3.90 秒仿真、2 次真实模型推理、53 个动作标量裁剪，官方 success=true，无运行错误。网页成功终止显示 phase=stopped，应以独立评分区分成功与预算停止。新电脑结果与旧机器 79 步记录分别保存。
- 证据：`runs/vla-recovery-web-20260916/20260916-151903-6413c69d` 的 manifest／events／末帧，以及 `.runtime/recovery/smolvla-web-acceptance.json`。验收脚本首次读到发布前的旧 ready 状态，修正为等待本地策略状态后只继续观察同一 episode，没有重复开始或多跑一轮。
- 后续：ACT 已完成 500 步接口验收且任务未成功，DP 正在同一官方初始化上实测；这些仍是单任务集成结果，不是整个 LIBERO 成功率。

### 2026-09-16 · M11 · 本地 VLA 环境与权重恢复（进行中）

- 目标：在用户已人工验收的主环境／LIBERO 环境之上，恢复独立 `.venv-vla` 与三个固定检查点，验证本地 LeRobot 的 CLI 和网页入口。
- 授权与范围：用户本次明确要求下载 SmolVLA、DP 等原有模型；按仓库目录恢复 SmolVLA、ACT、Diffusion，不训练、不发云端模型请求、不把旧电脑任务成功记录当作本机结果。
- 安装：使用 `python scripts/setup_vla.py --uv .venv/bin/uv --models smolvla-libero act-libero diffusion-libero`；下载缓存位于仓库忽略目录，禁用隐式 Hugging Face token 并移除继承的云端凭据变量。可用磁盘约 63 GiB；既有本地修改保留。
- 环境进展：独立 Python 3.12.14、LeRobot 0.4.4、PyTorch 2.10.0、torchvision 0.25.0、transformers 4.57.6、NumPy 2.2.6、huggingface-hub 0.35.3 安装完成；`uv pip check --python .venv-vla/bin/python` 检查 100 包无冲突。MPS built／available 为 true，实际 MPS 张量运算通过；已有本地策略／LIBERO 的 19 项离线回归通过。
- 下载障碍：固定版本 HF CLI 因远端文件 HEAD 元数据缺少 commit 报 FileMetadataError，单线程重试仍失败；磁盘检查显示未取得完整模型文件。进度条的 8/9 不代表已成功保存 8 个文件，正在核验公开来源并恢复下载，不能将环境安装完成误作模型可用。
- 处理进展：公开固定版本 API 可提供 commit、字节数、Git blob／LFS SHA256；改用官方固定 revision 的 GET 流式下载到 `.partial`，校验完整字节数与摘要后才落盘，保留依赖版本和检查点，不跳过完整性检查。辅助程序和证据留在 `.runtime/recovery/`，没有修改上游或模型文件内容。
- ACT 先行实测：固定 spatial task 0／init 0／seed 0、显式 MPS、上限 500 步；严格权重加载通过，500 次动作／500 个控制步、5 次真实推理、25 秒仿真、66.13 秒墙钟，无运行错误，官方 success=false，预算结束。该结果仅验证当前检查点／初始化；不能概括 ACT 方法整体能力。SmolVLA／DP 继续下载与检查。
- 后续：核验固定版本／权重完整性和三环境隔离；各模型先做少量 MPS 推理，再验证网页本地策略。策略只接收任务语言、双相机与机器人本体状态，评分独立。

### 2026-09-16 · M10c · 执行反馈选题预检（讨论材料完成，题目未定）

- 目标：在约两周范围内收敛可证伪问题，避免直接复制“反馈有用”的已有结论。
- 依据：核验 Show-Harness、AxisGuide、Reflective VLA、本体状态接口研究的 v1，以及 Orchestrating Robot Policies 的最新 v2（2026-09-06）。来源和论文／推断区分见[选题材料](../research/2026-09-16-question-scoping.md)；本次不是充分查新或论文复现。
- 候选：相同首次欠执行之后，冻结 VLM 能否利用后续可识别的命令—实际位移历史区分持续增益偏差与一次性失败，并做出原因相符的修正；判断／动作不一致可作为诊断读数。该候选没有被用户选定。
- 关键限制：先证明传感历史中存在可识别差异，避免要求模型从相同历史猜测未来；检查固定目标 servo 是否已消除扰动。保留同信息表达对照、时序信息消融及确定性修正器；不使用扰动标签、物体真值或评分作为策略输入。
- 后续：讨论可接受交付、负结果、模型和调用上限，再形成一页假设／判定表。建议先做零模型调用的可识别性预检，但本次尚未实施；没有自动启动付费实验或训练。

### 2026-09-16 · M10b · 独立 LIBERO 与双网页交付（完成）

- 安装：按 `scripts/setup_libero.py --uv .venv/bin/uv` 安装独立 `.venv-libero`；Python 3.10.21、MuJoCo 2.3.7、NumPy 1.23.5、PyTorch 2.2.2，固定源码 `8f1084e3132a39270c3a13ebe37270a43ece2a01`，`uv pip check --python .venv-libero/bin/python` 检查 59 个包无冲突。主环境仍为 Python 3.12.14／MuJoCo 3.12.0／NumPy 2.5.3。
- 实测：`MANILOOP_TEST_LIBERO=1 python -m pytest tests/test_libero_integration.py -q` **13 passed，70.27 秒**。覆盖五个任务目录（10／10／10／90／10）、重置一致性、128／256／512 图像配置、原生运动、目标位姿、夹爪保持与开合、传感与评分分离。CLI spatial 列出 10 项，LIBERO smoke 两路 RGB 成功；不是逐一验证全部任务物理可解或模型成功率。
- 已知兼容：直接在仿真环境 `import mujoco` 会触发旧版 macOS CGL 路径错误；安装文档要求通过主环境启动 worker，现有 worker 的 GLFW 兼容路径及实际渲染均通过。没有修改上游源码或官方协议。版本盘点使用包元数据，避免把直接导入检查误作正式运行路径。
- 网页：独立聊天 `http://127.0.0.1:8769/chat`；LIBERO `http://127.0.0.1:8767/`。浏览器已实际打开，两页默认手动配置且 options 返回空配置列表。LIBERO task 0／init 0、10 项 spatial 目录与两路 512×512 相机显示正常；初始为 0 次调用、0 秒仿真、未配置 Key。随后页面出现 2 次 mock 决策，只读健康检查为 completed／无错误／无在途请求；助手未触发该任务，也未重置当前场景。
- 隔离：两服务启动命令显式移除继承的 OPENAI_API_KEY、OPENAI_BASE_URL、OPENAI_MODEL、ARX_CONFIG_PATH。助手未读取私人配置或发起付费模型请求；未创建 `.venv-vla` 或 `.runtime/models`。LIBERO 所需 PyTorch 仅为读取官方初始化文件，不是下载策略权重。
- 证据：本机安装日志、两环境 freeze、硬件与版本摘要、离线／集成／smoke 日志在被忽略的 `.runtime/recovery/`，渲染在 `artifacts/`。旧电脑约 253 MB 原始记录没有恢复；当前新生成的 mock／smoke 文件不能替代旧轨迹。
- 交付：修改保存在 `fix/new-machine-recovery` 分支，未推送；历史不重写。两服务随当前进程保持运行，重启电脑后需从仓库根目录重新启动，不是开机自启服务。
- 后续：用户自行填写 Key／config，先做真实图像与动作格式分层诊断，再讨论单次小位移和研究预算。旧文字聊天通过、本地 SmolVLA／Diffusion 单任务成功仅为历史结果；本机真实云端视觉、结构化动作、自主抓放和完整 LIBERO 成功率未验证，Show-Harness 九动作仍未实现。

### 2026-09-16 · M10a · 主环境与网页启动隐私（完成）

- 变化：`.venv` 使用仓库内 Python 3.12.14，按 requirements-dev 安装固定依赖；两页默认手动配置，options 默认不扫描本地路径，用户显式发现后才扫描，主动检查配置／请求列表／发起请求时才读取所选配置。更新 [聊天说明](../API_CHAT.md)，保留主动导入和 CC Switch 功能。
- 验证：`python -m pip check` 无冲突；主 CLI 列表正常；`python -m pytest -q` 实测 **144 passed、13 skipped、58 subtests passed**。新增两个本机 HTTP 回归使用临时假 home／配置／认证验证默认零读取，不接触私人文件、不调用真实供应商。
- 验证：离线矩阵 8 组全部 completed，`is_mock=true`、`success=false`；Panda 与 ARX5 的 external／wrist RGB 均完成 smoke，已检查渲染图像。两页内联 JavaScript 语法和 `git diff --check` 通过。
- 桌面限制：沙箱运行 HTTP 测试报 loopback bind PermissionError，渲染报 invalid CoreGraphics connection；审批后在正常桌面环境运行均通过。MuJoCo 提示 ARB_clip_control 不可用，深度精度可能受限；本次 RGB 通过不代表定量深度精度验证。
- 网页：独立聊天服务已在 127.0.0.1:8769 启动，浏览器显示手动配置、Key 为空、尚未检查、等待发送。启动时显式移除继承的 API Key／地址／模型／配置路径变量。
- 边界：LIBERO worker 的传感字段与独立评分 RPC 保持原样；`/api/state` 是含评分的人类调试界面，不能整包交给未来策略。真实云端文字仅保留旧机器历史通过状态，本机未发云端请求；图像、动作和自主抓放仍待验证。
- 后续：LIBERO 固定源码已取得，继续独立 Python 3.10 安装、真实集成与网页验证；不安装 VLA。

### 2026-09-16 · M10 · 新电脑环境恢复（进行中）

- 目标：按最新交接恢复主环境、独立 LIBERO 环境和两个本机网页；不恢复旧虚拟环境，不下载 VLA 权重，不发送付费模型请求。
- 仓库：本机项目目录没有已有仓库；从官方远端克隆 `main`，初始工作区干净，HEAD 与 `origin/main` 均为 `1d5ea30`。已实际设置当前仓库提交身份为 Daniel-rmc / 2459944653@qq.com；历史未重写。
- 环境：实测 macOS 15.7.9、arm64、Apple M4（10 核 GPU）、24 GiB 内存。仓库内安装 uv 0.12.15 与 Python 3.12.14，并新建 `.venv`；依赖安装及运行检查尚在进行。
- 发现：原仿真网页初始化会自动发现配置并读取选中的本地配置，聊天页的 options 也会触发配置发现；为满足用户自行填写凭据的要求，正在改为显式操作。发现来自源码审计，没有读取本机私人配置或凭据。
- 难点与处理：沙箱禁止连接本机网络代理，首次 clone／依赖下载失败；通过工具审批后正常联网下载，未修改系统代理配置。
- 数据边界：旧 `runs/`、模型权重和私人配置没有随 Git 恢复；文档保留的历史单任务成功不等于新电脑验证，也不代表整个 LIBERO 成功率。
- 后续：完成主环境离线与渲染、LIBERO 任务／集成检查、网页启动及凭据读取回归，逐项追加实际证据；研究题目和预算仍未决定。

### 2026-09-16 · M9a · Git 提交邮箱与账号关联修正（本地与新提交完成）

- 目标：修复后续提交的 GitHub 作者归属，保留用户手动修改的交接文档。
- 证据：GitHub API 显示原 `main` 提交的 author／committer 未关联账号，contributors 中 10 条提交归为 Anonymous；用户给出的新邮箱是账号已验证的主邮箱。
- 变化：将当前仓库实际 `user.name`／`user.email` 设置为作者确认的身份；保留 HANDOFF 的邮箱更正，补充“文档内容不等于 Git 配置、每个新克隆需重新设置”的说明。
- 验证：`git var GIT_AUTHOR_IDENT` 与 `git var GIT_COMMITTER_IDENT` 均使用新邮箱；提交 `dba2bc1` 已推送并与远端一致，GitHub API 的 author／committer 均关联到 Daniel-rmc，contributors 已出现该账号。本次不改运行代码，不进行模型调用。
- 决策：用户明确选择保留历史，并自行在 GitHub 账号添加旧邮箱。保留原 10 条提交的邮箱与编号，未执行历史重写或强制推送，另一台电脑的已有克隆可正常 pull。
- 经验：推送权限、提交作者姓名与 GitHub 贡献归属是三件不同的事；关联邮箱应在首次提交前核对。
- 后续：旧邮箱由用户在 GitHub 的 Settings → Emails 添加；以账号设置和 GitHub 的重新计算结果为准。当前只验证了新提交关联，未宣称旧 10 条已全部归属；页面统计可能延迟刷新。

### 2026-09-16 · M9 · 跨电脑迁移交接（完成）

- 目标：确认代码可从 GitHub 恢复，让新电脑按一份文档重建环境并继续工程与论文讨论。
- 变化：新增 [HANDOFF.md](../HANDOFF.md)，README 增加迁移入口；列出 macOS／Linux／Windows 主环境、独立聊天、LIBERO／VLA 安装与检查命令、关键代码、研究候选和未确认事项。
- 同步依据：迁移前直接读取远端 `main` 为 `d3530ed699f6a3dc9fae152dfa60bb59e5bd6040`，与本地一致，工作区干净。交接资料随本条作为后续独立提交上传；最新交接提交可通过 `git log -- docs/HANDOFF.md` 定位，并与远端核对。
- 验证：当前 Python 3.12.14 的主 CLI、组件列表和两个安装器帮助命令正常；按源码核对参数，确认 Windows 不激活环境时显式指定 uv。交接文档 19 个相对链接、代码入口、20 个代码围栏及 Bash 示例语法检查通过，入口链接、空白和敏感路径扫描通过。仅文档变更，不重装环境或重复付费推理。
- 难点／经验：Git 不包含虚拟环境、可选上游安装、权重、原始 runs 和私人凭据。旧机器约 253 MB 原始轨迹需要用户按需另行保留；文档提供私下备份步骤，本次未自动创建或上传压缩包。
- 决策：研究讨论中的候选、推荐预算和时间安排仍未确认，交接不得将其当作已决定方案或付费实验授权。保持已有传感器赛道和独立评估边界。
- 后续：新电脑重新配置 API，先完成离线／渲染／文字／图像／格式诊断，再收敛论文假设；新机器的测试结果需按实际运行补记，不能沿用旧电脑通过状态。

### 2026-09-16 · M8a · Show-Harness 的 agent 能力归属（完成）

- 目标：回答主控制链是否复用了 Codex／Claude Code 等厂商 agent，并把可选入口与论文主框架分开。
- 变化：在原 [分析报告](../research/SHOW_HARNESS_ANALYSIS.md) 第 3.4–3.8 节追加调用链、三类能力分工和 ManiLoop 适配建议；保留已有工程方案，新增研究目录索引。
- 验证：静态追踪 launcher → VLMClient → requests POST /chat/completions，以及自建 GPTWebOperator → GUMI /api/step；独立复核上游固定提交 `137d5718c3b7af0150764d8f9beeb252c9f2794a`。主路径没有经过 Codex／Claude Code runtime；GUMI 文档确实提供外部 computer-use agent 入口，源码提到 Claude 浏览器扩展。仅文档变更，未运行上游或新增付费模型请求。
- 文档检查：46 个引用定义、对应 31 个上游源码文件及行号、37 个仓库相对链接通过检查；JSON 示例、代码围栏、敏感路径扫描与 git diff --check 均通过。
- 难点与发现：compact_model_state 过滤 sim_scene 几何字段，但保留 task_done/can_stop；单臂模拟后端 task_done 来自 scene.task_success()。过滤坐标不等于无评测真值泄漏，该结论仅适用于已核对的 GUMI 路径。
- 经验／决策：模型服务、agent runtime、机器人解释器和辅助机制分别记录；策略页面状态与人类调试状态分离。直接请求模型 API 不会自动获得厂商 agent 的工具与运行循环。
- 后续：原方案仍待实现；若增加外部 agent 适配器，使用独立实验组，并审查所有成功信号及派生门控。

### 2026-09-16 · M8 · Show-Harness 工程分析（完成）

- 目标：剖析 VLM 控制机器人机制，给出适配 ManiLoop 的工程方案，保持传感器赛道和独立评估边界。
- 变化：新增 [分析报告](../research/SHOW_HARNESS_ANALYSIS.md)，包含控制链路、输入与动作约定、插件、API 错误处理、微调与采集协议，以及文件职责、配置示例、里程碑和对照实验。README 增加入口；没有修改运行代码。
- 依据：上游固定提交 `137d5718c3b7af0150764d8f9beeb252c9f2794a`，论文 `arXiv:2609.10522v1`；对照 ManiLoop `e3fcb0e`。本次只读分析，没有执行上游机器人程序、下载模型或发送付费推理请求。
- 验证：核对论文架构图、主结果表和动作约定消融图；报告的 33 个固定源码文件链接与行号范围均在对应 checkout 检查通过，10 个仓库相对链接存在，JSON 示例可解析；私人路径与密钥格式扫描未发现问题。仅文档变更，不重新运行无关仿真测试。
- 难点与发现：上游零样本路径以计划结束置 success=true，微调真机路径则保留 success=false；解析失败可能沿用方向或默认下降；动作量、相机方向与训练模板具有特定平台约定。公开默认配置与论文部分设置不同，因此不能把默认运行当作论文复现。
- 经验／决策：优先验证“动作词—相机变化—实际运动”的一致性，复用现有 PoseTarget；先单动作，再消融规划、恢复与动作块。报告清楚区分源码事实、作者结果、上游测量和工程建议。
- 后续：按报告从图像／格式诊断、方向标定和九动作适配开始实施。尚未验证 Show-Harness 本机推理、语义策略在 LIBERO 的任务成功率或真机能力；本里程碑不代表这些功能已完成。

### 2026-09-16 · M7b · 独立聊天与供应商响应兼容（完成）
- 目标：让用户先配置并聊天，观察真实回复及失败原因，解除仿真状态对通信排障的干扰。
- 变化：新增 `maniloop chat` 与 `/chat`；手动／TOML／CC Switch 配置、模型选择、两种 API 协议、受控多轮聊天、超时和输出设置；显示 HTTP 状态、完成状态、脱敏错误、用量与部分正文。聊天不接触机器人、不写持久聊天日志、不自动重试。
- 真实验证：用户填写 FC 配置和 Key 后，`gpt-5.6-sol` / Responses 返回完整中文回复。页面中介绍能力的请求耗时 18.028 秒、4507 tokens；随后算术请求返回「连接成功，17 + 25 = 42」，耗时 9.842 秒、4543 tokens，HTTP 200、status=completed。只证明文字通信和对话流程，不代表图像、动作格式或任务能力已验证。
- 难点与修复：FC 返回空的 error code/message；OpenAI SDK 3.8.0 的 ResponseError 自动增加 `misalignment=None`。旧校验将 SDK 新增字段视为供应商额外错误字段，从而误报失败。真实 SDK 离线回归先复现失败，再改用 `model_dump(exclude_unset=True)` 仅校验线上实际提供字段；显式额外字段、非空错误与不完整响应继续拒绝。历史机器人日志未保存原始响应，不能证明其每次失败均由此造成。
- 验证：完整离线回归 142 项通过、13 项可选 LIBERO 检查跳过、58 项子测试通过；独立页面经本机模拟 HTTP 服务验证 Responses、Chat Completions 和不完整回复展示。没有为排查自动追加付费重试。
- 经验／决策：HTTP 成功、协议完整和动作可执行是不同层次；兼容性问题必须通过真实 SDK 反序列化测试，不能只用简单对象代替。网页使用纯文本渲染模型回复，Key 不进入日志和持久存储。
- 后续：保持当前聊天配置供用户继续测试；在修复后的机器人入口逐级验证图像和结构化动作，再评估任务成功率。模型列表失败不应阻止手填模型名称使用。

### 2026-09-16 · M7a · LLM 请求诊断与目标闭环（完成）
- 目标：实现已讨论的 P1/P2，让云端请求可诊断，并使一次 LLM 目标获得有界本地反馈执行。
- 变化：可调超时／推理强度和独立时间预算；文本、单图、双图动作格式三种诊断；失败耗时和脱敏类别、实际请求参数记录；终止场景在推理前重置；LLM 512 像素观测、固定目标位姿跟踪及执行终态反馈。CLI、网页和实验比较组同步。
- 验证：`MANILOOP_TEST_LIBERO=1 python -m pytest -q` 最终为 142 项测试、55 项子测试通过，包含 13 项真实 LIBERO 检查：小位移、旋转、512 像素渲染、夹爪保持与开合。本地 HTTP 模拟供应商的三种诊断全部通过，各一次调用、0/1/2 张图像、无物理步进。网页实际 +Z 10 mm 达到约 9.31 mm，位置残差约 0.74 mm。新版网页启动通过，双相机均为 512×512，初始仿真时间及 API 调用数为零。
- 远端验证：实现提交 `3ff9985` 的 [GitHub Actions](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35051694421) 四项全部通过：Windows、macOS、Linux 离线测试，以及 Linux OSMesa 的双机器人渲染。
- 用户复测：新版服务中，Astra 约 58.65 秒、Sol 约 53.83 秒后收到未通过完整性检查的响应；供应商报告总 token 分别为 3038、3231。两次均为 0 个执行动作、0 秒仿真时间。这已不同于原先的客户端请求超时；当前日志仍不足以区分响应状态、非空 error 或 incomplete_details 中的具体问题，不能认定额度不足或模型操作失败。下一步先分层诊断，必要时补充经过允许列表过滤的响应状态／原因记录，保持不执行不完整动作。
- 难点与处理：低层 OSC 的增量必须针对固定目标重新计算；不能重复相同增量。目标完成要等待速度下降，避免经过目标便结束。初始化夹爪使用官方预热的零命令，避免首次移动顺带张开夹爪。超时用量未知时记录 null，批量报告不将未知用量计为零或重复累计同一请求；诊断和正式任务分开标记。诊断后手动调试恢复物理步进；夹爪停止需满足本体稳定条件，不能仅等待固定步数。
- 经验／决策：只改明确的 LLM 包装层，保留官方控制器／缩放／任务评分与 VLA 节奏；512 像素、执行器容差及真正生效的请求选项进入比较组。诊断得到动作也只展示，不执行。
- 后续：用户填写配置后依次进行文本、图像和动作格式诊断，再复测 FC 的小位移及正式任务；随后按固定任务与初始化集合评估真实云端成功率。流式诊断、结构化视觉辅助和训练模式仍为后续工作。

### 2026-09-16 · M7 · 云端运行诊断与 v2 设计（诊断完成）
- 目标：解释用户真实云端运行失败，判断动作接口是否适合高延迟 LLM，并形成可执行的改进顺序。
- 证据：第一次从已结束环境发出请求并立即停止；第二次首次请求约 47 秒后超时，动作数和控制步均为 0。默认请求超时为 45 秒。日志不足以进一步定位网关／网络／推理阶段，更不能将此例计为模型操作能力失败。
- 变化：新增 [诊断与 v2 设计](../design/LLM_LOOP_V2.md) 和 `scripts/diagnose_libero_control.py`，为文档提供可复现实验；生产控制协议尚未变更。
- 验证：独立官方 spatial task 0 / init 0 / seed 0，无渲染、无 API、无物体真值输入。要求 Z 轴上移 10 mm，单步实际 1.1003 mm；随后 19 步零增量最终 2.3495 mm；20 步本体反馈追踪同一目标最终 9.8278 mm，三维残差 0.1926 mm。这是控制器诊断，不是模型任务成功。
- 难点与处理：128×128 图像细节有限；一条云端动作只覆盖 0.05 秒，易把「接受设定值」误解成「到达目标」。请求失败的耗时记录和最终生效参数记录也不足。保留证据，将网络排障、控制语义和视觉表示分开验证。
- 经验／决策：LLM 宜选择有界短程目标，本地执行器依据传感反馈追踪固定目标；VLA 保留训练匹配的原生动作节奏。新增协议显式编号、分组比较；不能通过重复增量或读取真值隐藏地提高成功率。
- 后续：P1 请求诊断、生命周期和预算可见性；P2 LLM 图像配置与目标跟踪；P3 固定任务／初始化集合的真实模型评测。尚未复测 FC 请求、实现 v2 或验证云端抓取成功率。

### 2026-09-16 · M6a · 首次跨平台 CI 修复（完成）
- 目标：修复公开仓库首次 GitHub Actions 暴露的 Windows 差异，使同一离线测试套件可跨平台运行。
- 证据：[首次运行](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35046906364) 的 Linux、macOS 离线测试和 Linux OSMesa 双机器人渲染通过；Windows 出现 6 项失败、2 项错误。
- 变化：测试读取中文日志和清单时显式指定 UTF-8；为无效／超大 TOML 输入设置简短参数名称，保留原有完整输入和安全断言。
- 验证：本机 `python -m pytest -q` 为 119 项通过、7 项可选集成检查跳过、49 项子测试通过；`git diff --check` 通过。修复提交 `27092ba` 的 [GitHub Actions](https://github.com/Daniel-rmc/ManiLoop/actions/runs/35047335420) 四项检查全部通过：Windows、macOS、Linux 离线测试，以及 Linux OSMesa 的 ARX5 / Panda 渲染。
- 难点与处理：Windows 默认文本编码不能解码中文记录；pytest 将超大参数生成的测试名称写入环境变量时超过 Windows 长度限制。修复测试的跨平台假设，不改变产品行为或减少覆盖。
- 经验／决策：UTF-8 文件的读取端也应声明编码；大输入测试应提供短且可读的参数名称，避免将完整负载写入测试标识和日志。
- 后续：README 环境要求与验证文档已同步实际检查结果。可选 LIBERO Linux 工作流和 Windows 渲染仍未验证；后续功能按独立提交、CI 和工作记录维护。

### 2026-09-16 · M6 · Git 管理与 GitHub 发布（完成）
- 目标：建立可追踪提交并同步到 Daniel-rmc 的公开 ManiLoop 仓库，沿用 MIT 许可证与第三方来源声明。
- 范围：源码、测试、文档、安装脚本、模型资产与工作记录纳入 Git；下载的策略权重、运行环境、实验输出、私人配置保持忽略。
- 发布前检查：185 个候选文件约 48.51 MiB，最大文件约 4.30 MiB；扫描未发现凭据格式、私钥或个人绝对路径，未纳入模型权重和运行环境。已有本地完整回归为 126 项测试及 49 项子测试通过。
- 决策：提交作者按用户指定配置为 Daniel-rmc，仅设置当前仓库。初始提交保存当前完整可运行平台；后续按独立功能或修复提交，并在里程碑同步工作记录。
- 发布结果：公开仓库 [Daniel-rmc/ManiLoop](https://github.com/Daniel-rmc/ManiLoop) 已创建；初始提交 `ed8f73c` 已推送，`main` 跟踪 `origin/main`，本地工作可按正常分支和 PR 流程持续同步。
- 验证与边界：发布前保留此前通过的 126 项本地测试及 49 项子测试；上传自动触发普通 GitHub Actions，远端运行结果以 [Actions 页面](https://github.com/Daniel-rmc/ManiLoop/actions) 为准。可选 LIBERO 集成工作流保持手动触发。原样保留上游 URDF，其中一处既有尾随空白未为格式检查而修改。
- 经验：已登录的应用连接器不一定支持创建仓库；本次使用系统现有 GitHub HTTPS 凭据完成创建和 Git 推送，未将凭据写入项目。
- 后续：按独立功能或修复提交，合并前检查 CI；继续维护本目录的里程碑记录。

### 2026-09-15 · M5b · 三类真实策略与交付验证（完成）
- 变化：正式仓库提供 `scripts/setup_vla.py`、固定模型目录、依赖清单、CLI 矩阵和网页选择器；README、VLA 安装说明、架构和第三方声明同步更新。模型、分词器和推理环境均在忽略目录，推理强制离线。
- 实测：同一 spatial task 0 / init 0 / seed 0，SmolVLA 79 步 / 2 次推理，success=true；ACT 500 步 / 5 次推理，success=false；Diffusion 108 步 / 14 次推理，success=true。模型完整权重均严格加载，三个家族均无运行错误。详见 [实验报告](../experiments/2026-09-15-libero-local.md)。
- 最终回归：正式仓库 `MANILOOP_TEST_LIBERO=1 python -m pytest -q`：126 项测试、49 项子测试通过；普通离线运行 119 项通过、7 项可选检查跳过。网页脚本语法、文档链接、Git 忽略与 `git diff --check` 通过；独立构建 wheel 成功且包含两个 worker、不含权重或运行环境。
- 交付验证：安装脚本在正式仓库重跑三个模型通过；7 项真实 LIBERO 集成检查通过，覆盖 256 像素 PNG、本体字段、重置、物理动作与任务目录。网页实际选择本地 SmolVLA 并开始执行，再次于 79 步获得官方成功判定；未发送云端 API 请求。
- 难点与处理：首次模型依赖下载耗时约 18 分钟；下载完后推理不需要网络。网页首次模型加载可能超过普通请求超时，给本地启动单独等待预算。再次开始本地任务时必须重置已完成的环境，增加对应回归。模型动作队列长度各异，因此明确区分控制步和真实神经网络推理。
- 经验／边界：DP 保留 100 次 DDPM 采样，耗时明显较长；不能为了展示速度而静默改采样设置。ACT / DP 属于非语言条件基线，SmolVLA 才是本轮语言条件 VLA。单任务成功不代表整个 LIBERO 成功率，也不代表跨任务、跨机器人或真机可用。
- 后续：扩大预先固定的任务和初始化集合，建立按策略协议分组的统计评测；按用户“先完成，再完善”的准则，本轮不增加训练框架或自动插件系统。

### 2026-09-15 · M5a · 官方 SmolVLA 真实闭环（完成）
- 变化：独立 LeRobot 0.4.4 推理环境；固定三个公开检查点，SmolVLA 为官方，ACT / Diffusion 为社区。新增 Agent、离线推理 worker、256 像素无损观测配置及 CLI / 网页入口。
- 验证：官方 smolvla_libero 在 Apple Silicon MPS、libero_spatial task 0 / init 0 / seed 0 上严格加载完整权重；2 步检查通过，随后正式闭环 79 步、2 次模型推理、3.95 秒仿真时间，官方 check_success=true。ACT 同任务执行 500 步、5 次推理，25 秒预算内 success=false；两者均无运行错误。
- 难点与处理：SmolVLA 配置残留 state6，但保存的训练统计与 LeRobot 官方 LIBERO 处理器均为 state8；保留检查点文件，使用 8 维实际状态。训练图像需双轴翻转，不能直接把网页显示图送入模型。DP 必须每个控制步更新两个历史观测，因此由 LeRobot 管理动作队列，平台逐步提交动作。
- 经验／决策：任务成功和接口可用分别记录；裁剪后的归一化 OSC 动作显式计数。完整权重严格加载，关闭重复基础模型下载；本机推理不读取云端密钥。公共检查点的标题与训练声明不能保证性能。
- 后续：完成 Diffusion 实测、正式仓库复验、安装说明和实验报告。以上为单任务集成检查，不是论文成功率复现。

### 2026-09-15 · M5 · 本地真实策略验证（进行中）
- 目标：寻找已经使用 LIBERO 数据训练的可运行小模型，在本机通过正式 VLA 接口完成真实推理与仿真闭环。
- 范围：优先 LeRobot 官方 smolvla_libero；检查 ACT / Diffusion Policy 的公开 LIBERO 检查点；不下载 pi0.5 等大型模型。
- 已知条件：本机 Apple Silicon、16 GB 统一内存；仿真与模型推理分别使用独立环境。输入仍限定为相机和机器人本体感知，不传入物体或评分真值。
- 难点／待核对：检查点的状态维度、相机顺序、训练图像方向、动作反归一化和采样频率须与训练协议一致；不能仅因权重能加载就宣称接口有效或任务成功。
- 后续：固定模型与库版本，建立推理适配并记录延迟、动作和官方任务评分；按实际结果更新本条后续里程碑。

### 2026-09-15 · M4 · 持久安装、回归与交付（完成）
- 变化：正式仓库安装可选 LIBERO 环境；安装器为新环境下载仓库内 Python，不依赖临时验证路径，不修改全局 Python 命令；主 README、LIBERO 使用说明、架构、贡献规范与第三方声明均已更新。
- 验证：在 macOS Apple Silicon 的正式仓库执行 `MANILOOP_TEST_LIBERO=1 python -m pytest -q`：121 项测试、49 项子测试通过。普通离线运行对应 115 项通过、6 项可选集成检查跳过。
- 验证：`python -m maniloop benchmark --suite examples/libero-offline-suite.toml` 的两个初始化均正常结束；`benchmark --backend libero --no-render --max-calls 3` 通过；网页模拟 VLA 完成两次决策、两个动作采样。安装脚本重跑通过；wheel 构建通过并包含后端 worker；文档本地链接与 `git diff --check` 通过，依赖、上游资源及运行结果均被 Git 忽略。
- 难点与处理：安装器可能从 PATH 选中临时 Python，即使设置下载目录也不足以保证持久性；改为显式安装并定位仓库内受管理的 Python。LIBERO horizon 包含预热，增加边界回归，终止后不再发送物理动作。
- 经验／决策：用独立依赖、明确单位和单一物理时钟接入外部基准，比复制场景资源或模拟引擎对象更便于长期维护。按“先完成，再完善”，本轮不加入自动插件发现或尚无实测的通用训练框架。
- 已知边界：五个目录读取通过不代表全部任务逐一完成物理验证；实际动作与渲染重点验证 spatial task 0。mock 的任务成功率为零符合预期；未调用云端 API、未验证真实 VLA 或模型自主任务成功率。Linux 工作流已配置但远程 Actions 尚未运行；Windows LIBERO 未实机验证。没有发布或推送仓库。
- 后续：固定实验协议与初始化集合；先接一个真实策略并保留完整观测／动作／反馈记录，再逐批验证更多官方任务和真实 VLA。变更后继续依照本目录 README 维护工作记录。

### 2026-09-15 · M3 · CLI、网页与实验协议接入（完成）
- 变化：运行器使用 Environment 协议；新增 LIBERO worker / 工厂 / 原生 OSC 动作块；CLI 和网页支持官方任务集、任务与初始化编号；实验记录保存控制器、上游版本和任务文件摘要。新增安装脚本、依赖清单、使用文档和可手动触发的 Linux 集成工作流。
- 验证：114 项离线测试、49 项子测试通过；6 项真实 LIBERO 检查通过（同种子重置、图像、末端实际运动、评分及五个目录）；CLI 和网页模拟 VLA 均完成两次决策、两个原生动作采样，官方任务评分 false。没有调用付费模型 API。
- 难点与处理：动作块 tick 与物理 step 分开，防止重复推进；网页闲置冻结控制时钟；官方 horizon 包括 10 步预热，因此正式 episode 最多 990 步，记录中显式声明；网页坐标提示随后端切换为 world / base。
- 经验／决策：官方任务可用与论文协议可比是两个问题；本协议明确 paper_comparable=false。安装不能依赖临时验证目录中的 Python，安装器应优先创建仓库内持久运行时。
- 后续：正式仓库安装与启动复验、完整回归、Git 忽略和文档链接核对。真实 VLA 和模型自主任务成功率仍属后续研究。

### 2026-09-15 · M2 · 官方场景、渲染与原生动作（完成）
- 变化：固定 LIBERO `8f1084e3132a39270c3a13ebe37270a43ece2a01`；独立 Python 3.10 / robosuite 1.4.0 / MuJoCo 2.3.7 运行环境；JSON 子进程通信。
- 验证：macOS Apple Silicon 实际加载 `libero_spatial` task 0、init 0；官方初始化文件含 50 个状态；两路 128×128 RGB 渲染成功；一个 7 维原生 OSC 动作推进 0.05 秒；官方评分返回 false，符合非任务策略的检查目的。主程序原有 105 项测试、49 项子测试通过。
- 难点与处理：robosuite / BDDL 缺少隐式依赖 termcolor / future，已加入运行依赖；MuJoCo 2.3.7 的 macOS CGL 路径错误，通过避免加载其未使用的上下文解决，渲染仍由 robosuite 自己的 GLFW 上下文负责；不改官方模型和控制器源码。
- 经验／决策：LIBERO 导入时会询问配置路径，应在 worker 启动前创建独立临时配置；训练依赖不能照搬为仿真最小依赖。上游 step 的 done 主要表达任务成功，平台还须明确记录自己的时间预算。
- 后续：验证 CLI / 网页闭环、动作采样节奏、传感边界及可重复安装；尚未验证模型自主完成此任务。

### 2026-09-15 · M1 · LIBERO 接入范围与记录制度（完成）
- 目标：引入可运行的官方任务，同时为长期维护建立可持续工程记录。
- 变化：新增本目录及记录规范；后续接入采用可选的外部环境后端，保留现有自建任务。
- 验证：检查了当前仓库。现有运行器直接读取 `sim.model` / `sim.data`，因此不能只增加一个导入语句；需要把物理时钟与动作执行放回环境接口。
- 难点与处理：LIBERO 官方依赖固定较旧的 robosuite / NumPy，而主程序使用 Python 3.12。选择独立 Python 环境与子进程传感／动作通信，避免强行混装。
- 经验／决策：先让原生 Panda 官方任务真正运行，再连接模型。保留原生评分；本平台的 LLM 动作编排和扩展观测需要明确标记协议，不能直接声称复现论文结果。
- 后续：固定上游版本，完成安装、官方任务初始化、RGB 渲染与原生动作步进；仅向策略提供允许的传感字段。

### 2026-09-15 · v0.1 · 自建任务框架交付（已完成，补录）
- 变化：ARX X5 / Panda、两个桌面布局、抓放 / 推物、LLM API 与模拟 VLA、CLI / 网页共用闭环、传感数据隔离、独立实验记录。
- 验证：此前交付记录为 105 项测试与 49 项子测试通过；八种组合的接触物理检查通过。详见 [验证说明](../VALIDATION.md)。本条是历史记录，不代替新版本回归。
- 经验：不同夹爪的执行器命令单位与关节编码器单位必须分离；物理测试不能冒充模型自主成功率；不同控制和观测配置必须独立分组。
- 限制：真实 VLA、真机与云端模型自主成功率尚未验证。
