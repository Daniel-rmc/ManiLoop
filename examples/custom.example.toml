# Replace both placeholders with values from your provider.
# The endpoint must support images and structured outputs via Responses API.
# Preserve any provider-specific URL path. Enter the matching key in the web UI.
model = "YOUR_VISION_MODEL_ID"
model_provider = "custom"

[model_providers.custom]
name = "Custom provider"
base_url = "https://api.example.com/v1"
wire_api = "responses"
